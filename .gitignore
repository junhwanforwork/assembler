# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# playwright (e2e 기능 QA — ASS-180)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# supabase — 로컬 CLI 상태(project-ref·pooler-url 등). 커밋 대상 아님.
supabase/.temp/

# 레인 창구 파일 — /wave-prep(하달)·/lane-start(보고)가 워크트리 루트에 만드는 웨이브별 임시 파일
PACKET.md
REPORT.md
REPORT.md.seen

# typescript
*.tsbuildinfo
next-env.d.ts
