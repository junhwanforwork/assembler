---
feature: 플로우 맵 (그림1 목업)
e2e: e2e/flow-map.spec.ts
mockup: 사용자가 보여준 이미지 #1 (가로 페이지 지도 + 스윔레인 + 연결선)
---

# 플로우 맵 수용 시나리오 (그림1)

> `/goal`이 판정하는 목표. 데이터는 기존 Page·UserFlow(SAMPLE_GRAPH) 사용 — 생성 안 건드림.

## 수용 기준 (Given /preview 흐름 탭)

- [ ] **AC1 — 다이어그램(텍스트 리스트 아님)** — 화면들이 SVG 연결선으로 이어진 지도로 렌더(현재의 "A → B" 텍스트 목록 대체).
- [ ] **AC2 — 스윔레인 그룹** — 왼쪽에 요구사항/기능 단위 그룹 레인이 세로로 구분.
- [ ] **AC3 — 좌→우 노드 트리** — 진입 노드에서 시작해 왼→오른쪽으로 페이지 노드가 배치, 연결선(엣지)으로 흐름 표현.
- [ ] **AC4 — 색 위계** — 진입·핵심·하위 노드가 색/스타일로 구분(목업: 검정/진보라/연보라). DS 토큰만.
- [ ] **AC5 — 회귀 0** — 스모크·perf 유지, tsc·lint·build 0.

## DoD
`e2e/flow-map.spec.ts` 통과 AND `tsc·lint·build·npm run e2e·npm run perf` 전부 ✅.
노드 좌표·곡률 등 시각 마감은 별도 — 아침 육안.
