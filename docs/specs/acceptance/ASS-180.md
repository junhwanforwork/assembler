---
ticket: ASS-180
title: e2e 기능 QA 하네스 + perf 계측(RAIL 예산)
e2e: e2e/smoke.spec.ts · e2e/perf.spec.ts
perf: [flow-drag, inspector-commit, wireframe-load]
---

# ASS-180 수용 시나리오

> 워크드 예제 — `/goal` 기능 완료 조건이 이 파일의 DoD를 참조한다.

## 수용 기준

- [x] **AC1** — Given 비로그인 세션, When `/` 진입, Then 홈 대시보드가 렌더된다.
  - e2e: `smoke.spec.ts › 홈 대시보드가 렌더된다`
- [x] **AC2** — Given 픽스처 그래프, When `/preview` 진입, Then 탐색기 트리 + 흐름·문서 탭이 보인다.
  - e2e: `smoke.spec.ts › /preview 객체그래프 빌더…`
- [x] **AC3** — Given `/preview`, When 흐름→문서 탭 전환, Then 문서 탭이 선택된다(`aria-selected=true`).
  - e2e: `smoke.spec.ts › /preview 캔버스 탭 전환`
- [x] **AC4** — Given `/perf` 하네스(`?perf=1`), When 핵심 인터랙션 구동, Then perf 마크가 RAIL 예산 PASS로 출력된다.
  - e2e: `perf.spec.ts › flow-drag · inspector-commit · wireframe-load`

## 상태/엣지

- [x] AI 호출 0 — `/preview`·`/perf`는 픽스처(SAMPLE_GRAPH·SAMPLE_PROJECT)로만 렌더(실 opus 호출 금지).
- [x] Supabase 의존 제거 — `mockProjects`로 결정적 실행.
- [x] perf 플래그 off → prod no-op(계측 오버헤드 0).

## Perf

- [x] `npm run perf` — flow-drag p95≤40ms · inspector-commit≤120ms · wireframe-load≤2000ms PASS
  - ⏸ canvas-pan은 InfiniteCanvas 미배포 → deferred(ASS-033/034)

## 완료 정의 (DoD)

`e2e/smoke.spec.ts` + `e2e/perf.spec.ts` 전부 통과 AND `npm run perf` PASS(회귀0)
AND `tsc·lint·build` ✅ AND `tickets.md`에서 ASS-180 `[x]`+날짜.
