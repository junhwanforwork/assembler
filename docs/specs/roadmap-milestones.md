# Assembler 로드맵 마일스톤 — 웨이브의 종착점과 중단 규칙

> 확정: 2026-07-02 · "웨이브를 돌려 결국 무엇을 얻는가"에 대한 답의 단일 출처.
> 지표 정의는 [north-star.md](north-star.md)가 원본 — 여기 복제하지 않는다(스펙은 한 곳).
> 첫 검증 방식 = **본인 도그푸딩** (2026-07-02 사용자 확정).

---

## 왜 이 문서인가

웨이브가 "백로그 소진"을 목표로 돌면 끝나지 않는다 — 매 웨이브의 크로스체크·리뷰가 새 티켓을
낳기 때문에(ASM-027·028이 그 산물) 백로그는 자기 증식한다. 웨이브의 사유는 백로그가 아니라
**마일스톤 탈출 조건**이어야 한다. 이 문서가 그 탈출 조건과 중단 규칙을 정의한다.

---

## 마일스톤

### 사용자 100% 정의 (2026-07-06 창업자 확정 — 웨이브 계획의 최상위 기준)

> **1차 목표(= M1-F 탈출):** 아이디어 하나로 제품 하나를 전부 기획하고 — 모든 API(계획 포함)·
> 기획 문서·사용자 플로우·기능 명세·와이어프레임을 **로컬에서** 볼 수 있다. 도달 즉시 사용자
> 직접 기능 QA 시작(= 로컬 도그푸딩, M3 판정의 입력).
> **2차 목표(= 아크 2):** 깃 링크를 가져오면 그 안에 선언된 데이터·API를 비개발자가 전부
> 이해할 수 있다. v1 스코프 = **TS/Next 공개 레포 1종(assembler 자기 레포 기준)** — 범용
> 파서는 신호 확인 후.
> 이 정의에 따라 **배포(구 M2)·P6 결정은 아크 2 뒤로 후순위** — P6 기한("배포 전")은 그대로
> 따라 이동한다.

### M1 · 경험 완성 — 기능 완결 → 점검 → 갭 마감 → 디자인 고도화 → 실데이터 검증

> 2026-07-03 개정: 사용자 3단계 제안(기능 점검 / 디자인 고도화 / 전체 재점검+테스트)을
> M1의 하위 단계 A~E로 편입. 순차 진행 — 각 단계의 산출이 다음 단계의 입력이다.

#### M1-A · 루프 완결 (= 5차 웨이브, 패킷 발급 완료)
- **ASM-029** 변경 전파 시각화 — 영향 범위를 구조가 알려주는 유일 차별 자산
- **ASM-030** 내보내기 모달 — 연결을 살린 구현 컨텍스트로 루프를 닫는 출구
- **ASM-028·027** 배포 전 방어(rate limit·잔여 하드닝) — M2의 선행 조건
- 탈출: 아이디어→생성→편집→AI 변경(≤3 인터랙션)→영향 확인→내보내기 완주 e2e 고정.

#### M1-B · 기능 총점검 (ASM-032)
1. **assembler 스펙을 assembler에 시드**(로컬) — v1-spec·north-star의 요구사항→기능→페이지→
   API·DB를 Composer/싱크-인으로 입력. 점검용 실데이터 + 디자인 평가용 진짜 콘텐츠 +
   M2 도그푸딩 데이터를 겸한다.
2. 여정×각도 점검 매트릭스 — 온보딩 B·C 경로 × 5각도 × 편집·챗 변경·전파·내보내기·기록.
   체크리스트 원본 = editor-interactions.md. ux-audit 낡은 항목(3~5차로 해소분) 닫기 포함.
3. 탈출: 갭 리포트 산출. **CRITICAL/HIGH만 M1-C 편입**, 나머지는 M3 판정 대기(중단 규칙 2).

#### M1-C · 갭 마감 웨이브 (6차)
- **ASM-033** DocView 읽기 투사 — PRD 투사 렌더(편집 없음, north-star "투사 뷰" 원칙)
- **ASM-034** WireframeView 읽기 구조 렌더 — UIElement 구조(#46 계약 소비,
  flow-view-pattern 재사용, 실렌더·편집 아님)
- + M1-B 발견 CRITICAL/HIGH
- 탈출: 5각도 전부 "준비 중" 표면 0 — 시작부터 끝까지 빈 각도 없이 경험 가능.

#### M1-D · 디자인 고도화 (스코프 폭발 방지 구조)
1. **ASM-035** 기계 부채 소탕(자명, 진단 불필요) — TYPOGRAPHY 토큰 CSS var 노출
   (globals.css←design-tokens.ts 1:1) → font-size 하드코딩 ~100건 토큰 치환 → 세그먼트
   컨트롤 1벌 프리미티브화 → Tooltip/Chip 소비·스피너 통합·죽은 챗 CSS 삭제.
2. **ASM-036** 인지 진단→선별 실행 — ui-ux-designer + assembler-design 병렬 진단.
   기준 = ux-references.md 3사 + 확정 디자인 방향(중성 블랙+블루·플로팅 라운드·그라데이션
   금지) + 정보 위계·시선 흐름·인지 부하. 우선순위 리포트 → **사용자 승인 top-N만 실행.**
- 탈출: font-size 하드코딩 0 · 세그 문법 1벌 · 진단 HIGH 해소 · 사용자 시각 승인 1회.

#### M1-E · 전체 재점검 + 실데이터 테스트 (ASM-037)
- 시드 스크립트 신설(가상 제품 + M1-B assembler 스펙 픽스처화, x-session-id RLS 정합).
- **실 DB 여정 e2e 1벌 신설** — 생성만 픽스처 주입(AI 호출 0 원칙 유지), 저장·PATCH·전파·
  내보내기·싱크-인은 모킹 없이 실 dev+DB 관통. 기존 모킹 e2e는 계약 검증용으로 유지.
- /health 진단 스윕 + 회귀 비교로 마감.

**M1 최종 탈출 조건:** 실 DB 여정 e2e 통과 + 시작→끝 경험에 빈 각도·거짓 표면 0 +
M1-D 탈출 조건 충족.

#### M1-F · 로컬 QA 갭 마감 (9차 — 1차 목표 직결, 2026-07-06 신설)
- **ASM-050** 생성 planned API 제안 — 아이디어 단독 경로에서 API가 0인 구조적 갭
  (생성 컬렉션 6종에 apis 없음, API=싱크-인 전용) 해소. planned 배지로 추정임을 정직 표시,
  코드 연결 시 실제와 대조. **생성 계약 변경 — prompt-evaluator 골든셋 게이트 필수.**
- **ASM-048** suggestions 포커스 자동 발사 제거 — 로컬 QA도 챗 인풋 클릭마다 과금되는 경로.
- **ASM-051** 로컬 기능 QA 시나리오·체크리스트(1차 목표 완주 각본 — ASM-031의 로컬 선행판).
- 탈출: 아이디어→생성→5각도+planned API 로컬 완주 + 사용자 기능 QA 1회 착수 가능.

### 아크 2 · 코드 연결 v1 (2차 목표 — 2026-07-06 사전 확정)

- 깃 링크 ingestion(공개 GitHub) → **TS/Next 파서 1종**(라우트·스키마 추출, 자기 레포 기준)
  → 자동 싱크-인 매핑 → 비개발자 이해 레이어(db-learning 원칙: 구조=사실/설명=추론 분리).
- 규모: 웨이브 2~3. **착수 조건 = 1차 목표 QA 시작 후.** M3 판정 세션은 1차 QA 마찰 로그로
  이 아크의 범위를 미세 조정한다(판정 갈래 (b)를 창업자가 사전 선택한 상태).
- P7 규칙과의 관계: 이 아크는 MCP가 아니라 싱크-인 자동화 — "MCP는 P6 마감 전 금지"에
  저촉되지 않는다.

### M2 · 검증 가능 상태 — 배포 + 계측 (도그푸딩용 최소, **아크 2 뒤로 후순위** 2026-07-06)

- **ASM-011** Vercel 배포 — 함수 리전 icn1(서울, Supabase ap-northeast-2 정합) · env ·
  배포 게이트(tsc·lint·build·e2e). 도그푸딩용이므로 공유·온보딩 고도화는 하지 않는다.
- **ASM-031** 도그푸딩 시나리오·지표 기록 — **텔레메트리 인프라를 만들지 않는다.** 수동 측정:
  시나리오 문서(예: "기능 1개 추가 + 변경 3건 반영")를 배포 URL에서 수행하며 북극성 지표
  (변경 반영 소요 시간 · 변경 1건당 수정 지점 수)를 직접 기록. asm_activity 델타(ASM-019)가
  보조 증거.

**탈출 조건:** 배포 URL에서 시나리오 1회 완주 + 지표 기록 1세트.

### M3 · 판정 — 빌드 중단 게이트

- 도그푸딩 실사용 ≈1주 → **판정 세션**: 지표·마찰 로그를 근거로 다음 아크를 결정한다.
- 도그푸딩 중 발견한 마찰은 tickets.md에 `DOG-` 접두 티켓으로 **기록만** 한다 —
  웨이브 편성은 판정 세션에서만.
- 판정의 갈래: (a) 마찰 해소 웨이브 (b) 차별 자산 심화(그래프 뷰·MCP 등) (c) 외부 검증 확대
  (d) 신호 없음 → 빌드 중단, 포지셔닝/문제 재정의.

---

## 중단 규칙 (무한 반복 차단)

1. **웨이브는 마일스톤 탈출 조건에 기여할 때만 편성한다.** "백로그에 남아 있어서"는 착수
   사유가 아니다.
2. **크로스체크발 신규 티켓은 자동 승격 금지.** CRITICAL/HIGH만 현 웨이브에 편입, 나머지는
   백로그에서 판정 세션을 기다린다.
3. **M2 이후 기능 웨이브 금지.** M3 판정 대기 후보(그래프 뷰 B-1 · #44 플로우 노드 ·
   와이어 실렌더·편집 · MCP · ASM-015 잔여 · ASM-021)는 도그푸딩 신호가 지목할 때만 부활한다.
   (2026-07-03 승격분: 패턴 프리미티브 정리→M1-D, #46 계약의 읽기 렌더→M1-C.)
4. **닫힌 파트에는 티켓을 편입하지 않는다.** 아래 "파트별 100%·마감성 정의"에서 `닫힘`인
   파트는 그 파트의 **재개 조건**이 실제로 발화했을 때만 다시 연다. 재개 조건 없는 편입 요청은
   반려한다(CRITICAL 보안·데이터 유실 결함은 예외 — 즉시 편입).

---

## 파트별 100%·마감성 정의 (확정: 2026-07-05)

> "100%"는 기능 목록이 아니라 **검증 가능한 상태 문장**이다 — 기능 목록형 정의는 크로스체크발
> 티켓이 편입되며 무한히 자란다. 각 파트는 3요소를 가진다:
> **① 100% 상태** (측정 문장) · **② 판정 방법** (무엇으로 확인) · **③ 재개 조건** (닫힌 파트를
> 다시 여는 유일한 트리거). 이 3요소가 갖춰진 상태 = "마감성 있음".

### 제품(현 아크·v1)의 100%

> **실사용 도그푸딩에서 "변경 1건당 수정 지점 수 = 1"이 성립하고, 변경 반영 소요 시간이
> 손으로 하는 것보다 명확히 짧다는 것이 측정으로 증명된 상태.** (지표 정의 = north-star.md)

- 판정: M3 판정 세션(지표 기록 + 마찰 로그). 이 정의는 **반증 가능** — 지표가 안 나오면
  빌드 중단·문제 재정의가 정답이며, 그것도 아크의 정상 종료다.
- 제품의 100%는 "완성"이 아니라 **해자 명제 증명 지점**이다. 도달하면 아크가 닫히고, 다음
  아크(협업·MCP 등)의 100%는 M3 판정이 새로 정의한다. "기획은 완성이 아니라 진화"를 파는
  제품이므로 정적 완성 정의는 자기모순.

### 파트별 정의

| # | 파트 | ① 100% 상태 | ② 판정 방법 | ③ 재개 조건 | 상태 |
|---|---|---|---|---|---|
| P1 | **연결 모델** (목적의 심장) | 인과 사슬(화면→동작→API→데이터)에 고립 산출물 0 · dangling 참조가 저장 경계에서 구조적으로 거부(409) | 무결성 검사·저장 경계 테스트 green 유지 | 모델에 새 개체 유형 추가 시 그 개체만 | **닫힘(유지)** |
| P2 | **AI 생성** (가속기) | 정상 입력(4000자 내 + 코드-진실 참조)이 타임아웃·단발 실패 없이 생성되고 항상 파서 경계 통과 · 실패 시 FE 표면 정직(카피·아이디어 보존·재시도 가능). **"더 똑똑하게"는 정의 밖** — 생성은 경쟁사도 한다 | G-1 재현 시나리오 통과 + 실패 표면 테스트 고정 | 도그푸딩 실사용 실패율 신호만 | 마감 중(ASM-042) |
| P3 | **투사 — 읽기 5각도** | 5각도 전부 "준비 중"·거짓 표면 0 · 각 각도는 모델의 진실만 렌더(뷰 전용 원본 데이터 0) | M1-C 탈출(표면 전수 스윕) | 모델 스키마 변경 시 해당 각도만 | 마감 중(ASM-033·034) |
| P4 | **투사 — 편집** | 편집 가능한 모든 필드가 모델 필드 1:1 매핑으로 환원(#26 원칙) · 저장은 단일 경로(patchDesignScoped) 경유 · 뷰 로컬 상태로 스펙이 갈라지는 지점 0 | 편집 경로 전수 → 단일 저장 경로 경유 확인 | 도그푸딩이 특정 각도의 편집 마찰을 지목할 때 그 각도만(M3 판정 경유) | 부분(스펙·데이터 각도만) — **문서·와이어 편집 미착수는 위반 아님**(P3와 별개 파트) |
| P5 | **변경 전파** | 어떤 모델 변경이든 영향 범위 자동 표시 + 반영은 1회(수정 지점 1) | 도달(ASM-029·038) — 회귀 테스트 유지 | "수정 지점 >1" 실사례 발견 시. ASM-021(동시 편집)은 협업 아크 소속 — 이 파트 아님 | **닫힘(유지)** |
| P6 | **버전 + diff** (해자 ③) | v1.0→v1.1 구조적 diff가 보이고 "이번 버전에서 바뀐 것"이 질의 가능 | ⚠ **범위 미결 — 창업자 결정 대기.** 결정 기한 = **M2 배포 전**(diff 없이 도그푸딩하면 해자 명제의 반쪽만 측정) | — (정의 확정 전엔 마감성 없음) | **정의 공백 — 유일한 미결** |
| P7 | **내보내기·MCP** (해자 ④) | v1 100% = 연결을 보존한 스냅샷 내보내기(도달). **MCP는 P6 마감 전 착수 금지가 정의의 일부** — 버전 없는 MCP는 카피 | 내보내기 e2e 유지 | MCP는 P6 닫힘 후에만 · 내보내기는 도그푸딩 신호만 | 닫힘(내보내기) / 대기(MCP — "안 하는 게 100%") |
| P8 | **디자인** | 기계적 정합(font-size 등 하드코딩 0 · 컨트롤 문법 1벌) + 인지 진단 HIGH 해소 + 사용자 시각 승인 1회. **"더 예쁘게"는 정의 밖** | M1-D 탈출 조건 | 사용자 시각 반려, 또는 신규 표면 추가 시 그 표면만 | 절반(ASM-035 완료·036 대기) |
| P9 | **배포·계측** | 배포 URL에서 시나리오 1회 완주 + 북극성 지표 기록 1세트. 온보딩·공유 고도화는 정의 밖 | M2 탈출 조건 | M3 판정만 | 미착수 |

---

## 현재 위치 (2026-07-06 갱신)

- M1-A~C 완료(5~7차) · M1-E 레인 몫 완료(8차 1차 통합, /health 스윕은 2차 통합에). **지금 =
  8차 진행 중** — M1-D 잔여(ASM-036 top-19 실행) + 진단 HIGH(ASM-046·047). 2차 통합 + 시각
  승인으로 M1 마감.
- 다음 = **M1-F(9차: ASM-050·048·051) → 1차 목표 도달·사용자 로컬 QA 시작 → 아크 2(코드
  연결 v1, 웨이브 2~3)**. 배포(M2)·P6 결정은 그 뒤.
- 파트 관점: P1·P5·P7(내보내기) 닫힘 · P2 마감 대기(ASM-045 반영, 유료 재현 스모크로 판정) ·
  P3 닫힘 · P8 8차로 마감 시도. P6은 후순위로 이동(기한 = 배포 전, 불변).
