---
date: 2026-06-12
ticket_id: ASS-013
mode: 순차 릴레이
---

# ASS-013 — 타입 UIElement, Wireframe (SpecComponent/Block 통합)

스펙 출처: 승인 플랜 `~/.claude/plans/wireframe-purring-flask.md` + object-model.md·wireframe.md·mapping.md.
사용자 확정: ① UIElement에 `props` 추가(스펙 변경 — 문서 선행 갱신) ② result는 구조화 유니온(5종 kind).
Pre-check ⚠️ 2건: 스펙 선행 갱신 + 프롬프트 동기 / UIElementType 리터럴 = builder BlockType 10종 정확히 동일.
DoD: `Page.wireframeId`의 "ASS-013 예정" 주석 제거 (ASS-012 C 이관).

## §A — 초안 (Pragmatist)

- 스펙 선행 갱신 3건: object-model.md UIElement 표(props·type·result), wireframe.md 구조 표+예시 props, mapping.md TS 표기 1줄(표 형식 보존).
- assembler.ts: Wireframe·UIElementType·UIElementState·UIElementResult·UIElement 추가 (Page 뒤·PageFlowStep 앞, 플랜 코드 그대로). UIElementType 리터럴 = block-catalog BLOCK_DEFS 10종 대조 확인.
- DoD: `Page.wireframeId` "ASS-013 예정" 주석 제거 (출처 규칙 유지).
- prompts/assembler.ts: OUTPUT_SHAPE uiElements 교체 + SYSTEM에 UI ELEMENT CONTRACT 6줄 (type 10종·props 키 맵·result kind 5종·navigate toPageId 필수·장식 none·props 언어).
- 빌드 게이트: tsc ✅ lint ✅ build ✅

## §B — Guardian 리뷰·토론

지적 5건 (M3 · L2) — M3 수용 · L1 기각 · L1 수용:

- [M] mapping.md TS 표기가 타입과 모순("나머지 4종 { kind, detail }" — none은 detail 없음) — 반영: kind별 정확 표기로 정정.
- [M] wireframe.md `type` 행에 비존재 리터럴 `input` 잔존 — 반영: "BlockType 10종 — block-catalog 단일 출처" + 10종 나열로 교체.
- [M] ASS-019 이관 범위에 enum 검증 부재 — AI가 `"type": "input"` 오타 시 BlockRenderer assertNever 크래시 경로. 반영: tickets.md ASS-019에 enum 검증 + 안전 폴백(type→text, kind→none) 메모.
- [L] OUTPUT_SHAPE toPageId 오독 여지 — **기각**: UI ELEMENT CONTRACT의 "navigate면 REQUIRED"가 해소, shape의 JSON 형태 유지 우선. 비-navigate 잉여 키는 ASS-019 정규화가 흡수.
- [L] variant/status 허용값 미열거 — 반영: CONTRACT props 줄에 enum 추가 (1줄).

확인 통과: UIElementType=BlockType 리터럴·순서 동일(직접 대조), jsonb round-trip 안전, props 임의 키는 readProp 폴백이 방어, detail? 완화 타당, DoD 완료.
반영 후 빌드 게이트: tsc ✅ lint ✅ build ✅

## §C — Architect 리뷰·토론

지적 4건 (M1 · L3) — APPROVE, 전부 수용:

- [M] UIElementType 10종 리터럴 4곳 산재(builder.ts/assembler.ts/프롬프트/wireframe.md) — 드리프트 침묵 장애. 반영: `UI_ELEMENT_TYPES as const` 배열 + 파생 타입, 프롬프트는 `join(", ")`로 파생 (이제 코드 쪽 출처는 1곳).
- [L] UIElementType JSDoc에 ASS-016 미기재 — 반영: "의도적 중복, 의존 방향 builder→assembler" 명시.
- [L] `name` 인라인 주석 스타일 이탈 — 반영: JSDoc 통일.
- [L] CONTRACT에 비-navigate toPageId 생략·none detail 없음 미명시 — 반영: 1구절 추가 (mapping.md TS 표기와 1:1).

확인 통과: result detail 비대칭 근거 타당(읽힘) · 선언 순서·보조 타입 위치 선례 준수 · CONTRACT 위치/문체 기존 섹션 일관 · 스펙 3건↔타입 1:1 · props 유니온은 YAGNI 보류 적절.

## §최종

- 순차 릴레이: A 초안 → B 지적 5(반영 4, 기각 1) → C 지적 4(반영 4).
- 산출물: assembler.ts에 Wireframe·UI_ELEMENT_TYPES·UIElementType·UIElementState·UIElementResult·UIElement (+~55줄) · prompts/assembler.ts OUTPUT_SHAPE+UI ELEMENT CONTRACT(타입 배열 파생) · 스펙 3건(object-model/wireframe/mapping) 동기.
- 최종 빌드 게이트: tsc ✅ lint ✅ build ✅ (2026-06-12)
- 후속 이관: ASS-019(type/kind enum 검증 + 안전 폴백) · ASS-016(builder BlockType re-point) · ASS-014(assembler.ts "ASS-014 예정" 주석 정리 — 기존 DoD 유지)
