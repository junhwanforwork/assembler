---
date: 2026-06-12
ticket_id: ASS-015
mode: 순차 릴레이
---

# ASS-015 — 타입 UserFlow, Mapping, ProjectGraph (+ Page 캔버스 좌표)

스펙 출처: 승인 플랜 `~/.claude/plans/wireframe-purring-flask.md` + object-model.md·flow.md.
사용자 확정: Page에 `x`/`y` 직접 저장 (빌더 Screen 선례, FlowCanvas ~85% 재사용 전제).
Pre-check ⚠️ 2건: Project id 배열 이중 저장 — 플랜 권장안(컬렉션 정본)을 B/C 토론으로 확정 / Page.device는 ASS-056 별도 티켓 — 추가 금지.
Mapping은 저장 타입 없음 (스펙: UIElement 필드 + UserFlow edge로 표현).

## §A — 초안 (Pragmatist)

- assembler.ts: Page에 x·y(표현 필드 주석), UserFlowEdge·UserFlow·ProjectGraph 추가 (+~40줄). device 미추가(ASS-056 준수).
- ProjectGraph 헤더에 Mapping 명시("별도 저장 객체 아님 — UIElement 필드 + UserFlow edge"). 별도 Project 타입 미생성(헤더 필드로 수렴, YAGNI).
- object-model.md: Project 표 id 배열 5행 전부 `(파생)` 정정 + "직렬화 정본 = ProjectGraph, 컬렉션 순서 = 표시 순서" 1줄, Page 표 x·y 행.
- 프롬프트: project 항목 `{ id, name, description }` 축소, pages x/y 미추가 확인, userFlow 일치 확인.
- 자가 리스크: Page x/y required vs AI 원시 출력(좌표 없음) — ASS-019 그리드 채움 전 캐스팅 경계 (현재 소비자 없어 무해).
- 빌드 게이트: tsc ✅ lint ✅ build ✅

## §B — Guardian 리뷰·토론

지적 7건 (H2 · M3 · L2) — 전부 수용:

- [H] navigate↔edge 양방향 정합 ASS-019 미이관 — 반영: 정방향(edge 생성)+역방향(trigger 불일치·toPageId 불일치·fromPageId 역산) 메모.
- [H] Page.x/y 그리드 채움 미이관 + 파싱 직후 캐스팅 위험 — 반영: 그리드 공식 + "정규화 입력은 unknown, 보정 후에만 ProjectGraph 단언" 메모.
- [M] 스칼라/중첩 dangling 목록 미열거 — 반영: result.toPageId(불량 시 "확인 필요", none 둔갑 금지)·edge 3필드·nextStepIds 명시.
- [M] orphan 검증 부재 — 반영: 역참조 스캔 + 삭제 금지·"확인 필요" 보존 정책. Project id 배열 제거가 판정을 훼손하지 않음 확인.
- [M] `userFlow?` optional이 균질 직렬화 위반(부재 vs 빈 배열 제3 상태) — 반영: required 승격 + 정규화가 빈 UserFlow 채움 (columns 선례).
- [L] OUTPUT_SHAPE optional 키 컨벤션 — 반영: INTEGRITY에 "omit when not applicable — never invent ids" 1줄.
- [L] UserFlow.id 의도 미정의 — 반영: "공통 규칙 준수용 — 싱글톤, 참조처 없음" 주석.

ASS-019 메모는 분량 누적으로 하위 목록 재구성.
확인 통과: 3중 대조(project 축소·x/y 미포함·userFlow 형태), optional 직렬화 round-trip, 단일 출처 파생 유지.
반영 후 빌드 게이트: tsc ✅ lint ✅ build ✅

## §C — Architect 리뷰·토론

지적 5건 (M1 · L4) — APPROVE, 전부 수용:

- [M] OUTPUT_SHAPE `"project"` 래퍼 vs ProjectGraph 평면 루트 — 변환 지점 미문서화 드리프트 씨앗. 반영: **래퍼 제거, 루트 평면 정렬** (출력 JSON = ProjectGraph 1:1, seam 소멸 — 더 단순한 쪽 채택).
- [L] 파일 헤더에 직렬화 루트 미안내 — 반영: "직렬화 루트는 파일 말미의 ProjectGraph" 1줄.
- [L] "Project 직속 아님" 규칙과 top-level 컬렉션 오독 여지 — 반영: ProjectGraph 주석에 "평면 컬렉션 저장 — 소유는 id 참조" 1줄.
- [L] Page.x/y 주석에 채움 출처 누락 — 반영: "AI 미생성 — 정규화(ASS-019)가 그리드 배치로 채움".
- [L] object-model `userFlowId?`의 `?` 잔재 — 반영: 제거 + "항상 존재(빈 edges 가능)".

확인 통과: ProjectGraph 컬렉션 순서=섹션 순서 · 보조 타입 선례 · x/y 끝 배치(도메인/표현 분리) · userFlow required 근거 주석 · optional 키 컨벤션 ↔ TS optional 목록 일치 · ~191/400줄.

## §최종

- 순차 릴레이: A 초안 → B 지적 7(반영 7) → C 지적 5(반영 5).
- 산출물: assembler.ts에 Page x·y + UserFlowEdge·UserFlow·ProjectGraph (Phase 1 데이터 모델 완결, ~193줄) · 프롬프트 루트 평면 정렬 + optional 키 컨벤션 · object-model.md Project 표 (파생) 정정·Page 표 x·y.
- 핵심 결정: 컬렉션이 멤버십·순서의 정본(Project id 배열 저장 안 함) · userFlow required(정규화가 빈 값 채움) · 출력 JSON = ProjectGraph 평면 1:1.
- 최종 빌드 게이트: tsc ✅ lint ✅ build ✅ (2026-06-12)
- 후속 이관: ASS-019 메모 대폭 보강(하위 목록 재구성 — 정규화 입력 unknown·그리드 채움·스칼라 dangling·navigate↔edge 양방향·orphan 보존·userFlow 채움)
