---
date: 2026-06-12
ticket_id: ASS-011
mode: 순차 릴레이
---

# ASS-011 — 타입 Requirement, Feature

스펙 출처: `memory/tickets.md` Phase 1 + `.claude/rules/assembler/object-model.md`(단일 출처).
Pre-check ⚠️ 2건: (1) 미래 객체(Page/Api/Database) id 참조는 주석으로 대상 명시 (2) jsonb 직렬화 대비 `interface` 금지 — 객체 리터럴 `type`.

## §A — 초안 (Pragmatist)

- `src/lib/types/assembler.ts` 신설 (+30줄, 신규 파일 1개) — Requirement·Feature 두 `type` 선언.
- jsonb 직렬화 함정 회피로 `interface` 대신 `type` — 파일 상단 주석에 이유 명시.
- 미래 객체 id 필드(pageIds/apiIds/databaseIds)는 JSDoc으로 예정 티켓(ASS-012/014) 명시.
- 헬퍼·검증 로직 의도적 배제 — 소비자 없음, 선제 추상화 금지.
- 빌드 게이트: tsc ✅ lint ✅ build ✅

## §B — Guardian 리뷰·토론

지적 3건 (H1 · L2):

- **[H] Requirement↔Feature 양방향 이중 저장** — `relatedFeatureIds`(Requirement)와 `requirementIds`(Feature)가 같은 관계를 양쪽에 저장. jsonb엔 FK가 없어 드리프트 영구화. object-model.md 자체 모순(공통 규칙 "역참조 파생" vs Requirement 표 명시).
  - 토론: A 수용 — "이 요구를 *충족하는* Feature"는 Used By형 역참조. 정참조는 `Feature.requirementIds`.
  - 반영: ① 타입에서 `relatedFeatureIds` 제거(파생 주석) ② object-model.md Requirement 표 `(파생)` 마킹 ③ 생성 스캐폴드(`prompts/assembler.ts`)에서 제거 — AI가 양쪽을 생성하며 어긋날 경로 차단.
- [L] AI 불완전 JSON의 배열 누락 — 타입 소관 아님, ASS-019 검증 레이어 요구사항으로 이관(누락 배열 `[]` 기본 채움).
- [L] branded id·readonly 강화 — 과방어로 판단, 미적용 합의.

확인 통과: 필드 대조(누락/과잉 0), `type` 선언, 이름 충돌 없음, undefined 직렬화 손실 경로 없음.
반영 후 빌드 게이트: tsc ✅ lint ✅ build ✅

## §C — Architect 리뷰·토론

지적 1건 (L1) — **APPROVE**:

- [L] "ASS-012/014에서 추가 예정" 주석은 해당 티켓 완료 시 주석 부패. → 토론: 수용하되 코드 변경 없음 — ASS-012·ASS-014 DoD에 "assembler.ts 예정 주석 제거/갱신" 포함(tickets.md에 메모).

확인 통과: 잔여 9종 타입을 더해도 ~200줄(400줄 한도 내, 분리 불필요) · 네이밍 단일 출처 일치 · 파생 역참조 docstring 2단 구조가 이후 Api/Database 파생 필드의 선례로 적절 · prompts 스캐폴드 rules 동기 유지.

## §최종

- 순차 릴레이: A 초안 → B 지적 3(반영 1, 이관 1, 기각 1) → C 지적 1(이관 1).
- 산출물: `src/lib/types/assembler.ts` 신설 + `src/lib/prompts/assembler.ts` 1행 정정 + `rules/assembler/object-model.md` Requirement 표 `(파생)` 정정.
- 최종 빌드 게이트: tsc ✅ lint ✅ build ✅ (2026-06-12)
- 후속 이관: ASS-019(누락 배열 `[]` 기본 채움) · ASS-012/014(예정 주석 정리)
