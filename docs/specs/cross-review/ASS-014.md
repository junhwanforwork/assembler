---
date: 2026-06-12
ticket_id: ASS-014
mode: 순차 릴레이
---

# ASS-014 — 타입 Api, Database (전역 공유 객체)

스펙 출처: `.claude/rules/assembler/object-model.md` (Api·Database 표).
역참조 4종(`triggeredByElementIds`/`usedInPageIds`/`usedByFeatureIds`/`relatedApiIds`)은 스펙에 `(파생)` — 저장 필드 금지, 주석 안내만.
Pre-check ⚠️ 2건: method 형태(HTTP 5종 유니온 권장) · columns 내부 형태 미정(최소안 선택 후 토론).
DoD: assembler.ts "ASS-014 예정" 주석 6곳 정리 (Feature·Page·UIElement 각 2곳).

## §A — 초안 (Pragmatist)

- assembler.ts에 `API_METHODS`(const 단일 출처)·`ApiMethod`·`Api`·`Database` 추가 (UIElement 뒤·PageFlowStep 앞, +31줄).
- 역참조 4종은 저장 필드 없이 JSDoc 안내만 (파생 원칙).
- 형태 결정: method = HTTP 5종 유니온 / columns = `string[]` 최소안 (구조화 보류, YAGNI).
- DoD: "ASS-014 예정" 주석 6곳 전부 제거.
- 프롬프트 apis/databases 스캐폴드는 타입과 1:1이라 미수정. API_METHODS 프롬프트 주입은 리뷰 합의 사항으로 남김.
- 빌드 게이트: tsc ✅ lint ✅ build ✅

## §B — Guardian 리뷰·토론

지적 5건 (H1 · M3 · L1) — 전부 수용:

- [H] API_METHODS 프롬프트 미주입 — "프롬프트가 파생" 주석이 거짓 상태, method만 오염 무방비. 반영: import + `"method" must be exactly one of: ${API_METHODS.join(", ")}` (GENERATION CONTRACT).
- [M] method 폴백 방향 미정 — 소문자 `delete`→GET 둔갑 위험. 반영: ASS-019 메모 "대문자 정규화 후 매칭, 불일치 시 묵살 폴백 금지·확인 필요 마킹".
- [M] columns optionality 3중 경계 모호 — (a)안 채택: required `string[]` 승격(빈 배열 = 미기재), object-model.md 표 동기 정정. 균질 직렬화 + [] 채움 정책 정렬.
- [M] success/error 누락 보정 소관 미명시 — 반영: ASS-019 메모 "required string 누락 시 '확인 필요' 보정"(generation.md 계약 준용).
- [L] Database.name 언어 미규정 — 반영: 프롬프트 WRITING STYLE에 "snake_case 영어 테이블명·영어 route path" 1줄 + object-model.md 표기.

확인 통과: 필드 스펙 1:1 · 역참조 미저장 · 선언 순서 · const 단일 출처 선례 · JSON-safe · name 동명 충돌 기각(컬렉션 분리+id 식별).
반영 후 빌드 게이트: tsc ✅ lint ✅ build ✅

## §C — Architect 리뷰·토론

지적 3건 (M1 · L2) — APPROVE, 전부 수용:

- [M] object-model.md Api 표에 method 값 집합 미기재 — md→TS 파생 방향 역전. 반영: "GET/POST/PUT/PATCH/DELETE 5종 — TS API_METHODS 미러" 표기 (UIElement type 선례 동일).
- [L] ApiMethod JSDoc 부재 — 반영: 파생 유니온 + 5종 충분 근거 주석.
- [L] success/error ↔ UIElementResult 철학 비대칭 근거 미기재 — 반영: Api 헤더에 "설명 텍스트 — 그래프 연결 출처는 UIElement.result" 1줄.

확인 통과: 네이밍 선례(API_METHODS→ApiMethod = UI_ELEMENT_TYPES→UIElementType) · 선언 순서 · 프롬프트 join 파생 · method 줄 위치 적정(별도 섹션은 YAGNI) · columns required 양쪽 동기 · ASS-015 자리 여유(~150/400줄).

## §최종

- 순차 릴레이: A 초안 → B 지적 5(반영 5) → C 지적 3(반영 3).
- 산출물: assembler.ts에 API_METHODS·ApiMethod·Api·Database (+~35줄, columns required) · 프롬프트 method 제약/WRITING STYLE 2줄 · object-model.md Database·Api 표 정정.
- 최종 빌드 게이트: tsc ✅ lint ✅ build ✅ (2026-06-12)
- 후속 이관: ASS-019(method 대문자 정규화·묵살 폴백 금지, required string "확인 필요" 보정)
