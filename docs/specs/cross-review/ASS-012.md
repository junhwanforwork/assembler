---
date: 2026-06-12
ticket_id: ASS-012
mode: 순차 릴레이
---

# ASS-012 — 타입 Page, PageFlow

스펙 출처: `memory/tickets.md` Phase 1 + `.claude/rules/assembler/object-model.md`·`flow.md`(단일 출처).
Pre-check ⚠️ 2건: (1) 미래 객체(Wireframe ASS-013 / Api·Database ASS-014) id 참조 JSDoc 명시 (2) PageFlow Navigate→UserFlow edge 참조는 스펙 step 형태에 필드 없음 — 선제 추가 금지, 리뷰 토론 포인트.
DoD: `Feature.pageIds`의 "ASS-012 예정" 주석 제거 포함 (ASS-011 C 리뷰 이관분).

## §A — 초안 (Pragmatist)

- `src/lib/types/assembler.ts`에 `Page`·`PageFlowStep`·`PageFlow` 추가 (+33줄) — 스펙 필드 1:1, 추상화 없음.
- DoD 반영: `Feature.pageIds` JSDoc에서 "ASS-012 예정" 문구 제거.
- `pageFlowId?`만 옵셔널(스펙 표기 그대로), `wireframeId`는 필수 — 생성 순서 제약은 스펙(1:1 필수)을 따른 결과로 자가 리스크에 기록.
- PageFlowStep에 UserFlow edge 참조 필드 의도적 미추가 (ASS-015 영역).
- 빌드 게이트: tsc ✅ lint ✅ build ✅

## §B — Guardian 리뷰·토론

지적 3건 (L3, C/H/M 없음) — 전부 수용:

- [L] Page↔PageFlow 양측 저장 출처 미선언 — ASS-011의 "파생 역참조"와 달리 스펙이 정한 소유 관계라 위반 아님. 반영: `pageFlowId?` 주석에 "불일치 시 `PageFlow.pageId`가 출처, 검증 ASS-019".
- [L] 진입 step 규약 부재 — 반영: "steps[0] = 진입 단계"를 타입 주석 + flow.md 표에 명시.
- [L] `Page.wireframeId` 무결성 절 누락 — 반영: object-model.md 무결성 절에 역방향(`Page.wireframeId`/`pageFlowId`) 추가. ASS-019 dangling 검증 목록에 포함.

확인 통과: 직렬화 round-trip(optional 키 드랍 무해), 배열 필수 필드 형태 안정, 스펙 1:1 대조, steps 중첩은 스펙 내장 구조라 "중첩 금지" 위반 아님.
반영 후 빌드 게이트: tsc ✅ lint ✅ build ✅

## §C — Architect 리뷰·토론

지적 2건 (M1 · L1) — APPROVE WITH FIXES, 전부 수용:

- [M] 타입 선언 순서 규약 미고정 — ASS-013~015 삽입 위치 churn 예방. 반영: 헤더에 "선언 순서는 object-model.md 섹션 순서를 따른다 (보조 타입은 부모 직전)" 1줄. 현재 순서는 규약과 이미 일치라 이동 없음.
- [L] `wireframeId` 소유 출처 주석 비대칭(`pageFlowId`만 있음) — 반영: "불일치 시 Wireframe.pageId가 출처" 추가.

확인 통과: `PageFlowStep` 네이밍(부모 prefix + 부모 직전 선언)이 ASS-015 `UserFlowEdge` 선례로 적절 · 미래 티켓 JSDoc 포맷 5곳 동일 · 스펙 문서 수정 2건 기존 문체 일치 · 파일 64줄(한도 400).

## §최종

- 순차 릴레이: A 초안 → B 지적 3(반영 3) → C 지적 2(반영 2).
- 산출물: `src/lib/types/assembler.ts`에 Page·PageFlowStep·PageFlow 추가 + 헤더 규약 1줄, `flow.md` steps[0] 규약, `object-model.md` 무결성 절 역방향 2건.
- 최종 빌드 게이트: tsc ✅ lint ✅ build ✅ (2026-06-12)
- 후속 이관: ASS-019 dangling 검증 목록에 `Page.wireframeId`/`pageFlowId` 포함
