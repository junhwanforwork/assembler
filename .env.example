# 환경변수 템플릿 — 실제 값은 .env.local 에 채운다(.env.local 은 git 미추적).

# Anthropic — AI 그래프 생성 (ASS-E01). 없으면 /api/generate 가 503(해요체) 반환.
ANTHROPIC_API_KEY=

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# 앱
NEXT_PUBLIC_APP_URL=http://localhost:3000
ADMIN_PASSWORD=
