Review the code I just wrote or the files I specify. Check for:

1. Unnecessary abstraction or over-engineering for MVP stage
2. Duplicate logic that can be consolidated
3. Vague variable/function names (data, item, thing, temp) — rename them
4. Dead code or unused imports
5. Any logic that violates OPINION naming conventions (see CLAUDE.md)

Fix all issues found. Keep changes minimal — only simplify, do not add features.
